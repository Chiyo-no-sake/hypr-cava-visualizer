# Hyprland-format color definitions
# Used with: hypr-cava-visualizer --colors-file colors.conf
#
# The visualizer reads $primary, $secondary, and $tertiary variables.
# Format: $name = rgb(RRGGBB)

$primary = rgb(F2BF6E)
$secondary = rgb(E6BDC0)
$tertiary = rgb(B5CF9E)
